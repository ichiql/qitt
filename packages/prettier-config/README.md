## @qitt/prettier-config

```shell
pnpm add @qitt/prettier-config
```

```shell
npm install @qitt/prettier-config
```

```shell
yarn add @qitt/prettier-config
```

config file for prettier

package.json

```json:package.json
{
  ~~~
  "prettier": "@qitt/config/prettier"
  ~~~
}
```
